# FreeRTOS 的设计思想和基础应用


---

## 📋 目录

- [FreeRTOS 的设计思想和基础应用](#freertos-的设计思想和基础应用)
  - [📋 目录](#-目录)
  - [1. 背景：为什么我们需要 RTOS？](#1-背景为什么我们需要-rtos)
    - [1.1 先说说单片机是怎么跑程序的](#11-先说说单片机是怎么跑程序的)
    - [1.2 裸机有啥毛病？](#12-裸机有啥毛病)
      - [一、CPU 在「等」的时候啥也不干](#一cpu-在等的时候啥也不干)
      - [二、实时性差 —— 紧急事件得排队等](#二实时性差--紧急事件得排队等)
      - [三、代码耦合 —— 牵一发而动全身](#三代码耦合--牵一发而动全身)
      - [四、功耗控制难](#四功耗控制难)
      - [总结——痛在哪？](#总结痛在哪)
  - [2. FreeRTOS 是什么](#2-freertos-是什么)
    - [2.1 一句话 + 详细表格](#21-一句话--详细表格)
    - [2.2 市面上 RTOS 那么多，为啥选它？](#22-市面上-rtos-那么多为啥选它)
  - [3. 核心设计思想（重点）](#3-核心设计思想重点)
    - [Q: 等等，上面不是说裸机有那么多毛病吗？那 RTOS 是怎么治这些毛病的？](#q-等等上面不是说裸机有那么多毛病吗那-rtos-是怎么治这些毛病的)
    - [A:](#a)
    - [3.1 任务 —— 最基本的执行单元](#31-任务--最基本的执行单元)
    - [3.2 调度 —— 谁来跑、跑多久](#32-调度--谁来跑跑多久)
    - [3.3 状态切换 —— 任务都在干嘛](#33-状态切换--任务都在干嘛)
    - [3.4 ⭐ Blocked 状态 —— FreeRTOS 省 CPU 的关键](#34--blocked-状态--freertos-省-cpu-的关键)
      - [Q: 裸机的 delay 是什么？](#q-裸机的-delay-是什么)
      - [A:](#a-1)
      - [Q: FreeRTOS 的 delay 又是什么？](#q-freertos-的-delay-又是什么)
      - [A:](#a-2)
      - [Q: 那「任务切换快」又省的是啥？](#q-那任务切换快又省的是啥)
      - [A:](#a-3)
    - [3.5 优先级反转 —— 一个经典坑](#35-优先级反转--一个经典坑)
      - [场景](#场景)
      - [踩坑流程](#踩坑流程)
      - [💡 FreeRTOS 怎么解决：优先级继承](#-freertos-怎么解决优先级继承)
    - [3.6 任务间的同步与通信](#36-任务间的同步与通信)
    - [3.7 FreeRTOS 的设计哲学](#37-freertos-的设计哲学)
  - [4. FreeRTOS vs 现代操作系统](#4-freertos-vs-现代操作系统)
    - [现代 OS 能做啥？](#现代-os-能做啥)
    - [FreeRTOS 实现了其中哪些？](#freertos-实现了其中哪些)
    - [值得讲的点](#值得讲的点)
  - [5. 启动与文件结构](#5-启动与文件结构)
    - [5.1 完整执行流](#51-完整执行流)
    - [5.2 main() 里面发生了什么](#52-main-里面发生了什么)
    - [5.3 vTaskStartScheduler() 之后的世界](#53-vtaskstartscheduler-之后的世界)
    - [5.4 为什么非要用中断做切换？](#54-为什么非要用中断做切换)
      - [A:](#a-4)
    - [5.5 代码文件对应关系](#55-代码文件对应关系)
  - [写在最后](#写在最后)

---

## 1. 背景：为什么我们需要 RTOS？

### 1.1 先说说单片机是怎么跑程序的

在上次的[嵌入式,让我看看!](../../Build_Process/markdowns/嵌入式，让我看看！.md)中 , 静态编译型语言，最后都会通过编译器一步步的编译过程, 把它变成二进制机器码，烧进 Flash，上电后 CPU 一条一条取指令执行。

在裸机单片机上, 程序一般包括以下几个部分:(这里以 经典的按钮点灯 为例)

```c
#include <....>
#include <....>

int main(void) {
    LED_initial();
    kEY_initial();
    SETIAL_initial();//设备初始化, 包括对 部分

    while(1) {
        step1();   // 按键扫描
        step2();   // LED 刷新
        step3();   // 串口发送
    }
}
```

`main()` 进去以后，经过上机紧接着的一系列初始化后(包括startup.s和上文`main`函数内的初始化), 
就是一个`while(1){}` **死循环**，随后CPU的指令读取就在里面一圈一圈地转。这称为 **超级循环（Super Loop）**，是**裸机编程**最典型的样子。

大多数单片机入门都是从这种写法开始的，那这种工程有什么问题吗,
~~当然有, 不然下面讲什么~~
我们接下来分析分析

### 1.2 裸机有啥毛病？

#### 一、CPU 在「等」的时候啥也不干
对上文代码中的循环部分具体化(伪代码如下)
```c
while(1) {
    scan_key();    // 几微秒就扫完了
    delay_ms(10);  // ← CPU 在这 10ms 里啥也没干，纯数数
    toggle_led();
    uart_send();
}
```
> 这里要声明的, 为什么这里需要delay
因为需要`按键去抖`, 
简单说就是, 芯片内部通过读取电平特定跳变, 来判断按键是否被按下, 机械按键按下去的瞬间, 金属触点会像弹跳球一样"弹跳"几次——电平在稳定前会快速通断好几次（一般持续 5~10ms）。如果不 delay 一下等它稳定, CPU 可能把一次按下读成"按了→松了→按了→松了...", 那程序就会认为按钮被按了好几下, 然后LED也开关了好几次

所以用 delay 等 10ms 是合理的——**但问题在于**, CPU 在这 10ms 里**只能空转**, 啥正事也干不了。

为了解决这个问题, 我们要先搞懂——
`delay_ms(10)` 的本质是什么？

```c
void delay_ms(uint32_t ms) {
    for(uint32_t i = 0; i < ms * 1000; i++);
}
```

CPU 在这 10ms 里不停地 `i++`，**别的啥也不干**。按键按了？不知道。传感器来数据了？不知道。中断来了？虽然能响应，但处理完一回来，它继续数数

> 🍳 **类比一下：** 就像厨师在等水烧开的时候，盯着水壶发呆，而不是趁这个时间切菜、洗锅。

#### 二、实时性差 —— 紧急事件得排队等

假设这么个需求：
- 按键按下，必须在 **5ms 内** 响应
- 但 while 循环里有一步特别慢（比如刷 OLED，要 20ms）

```c
while(1) {
    oled_display();    // 20ms , 这时按键按下
    scan_key();        // chovy, 大哥你快点啊， 我花都要谢了（bushi）
    process_sensor();
}
```

<span style="color:red">注：</span> **响应时间不由「紧急程度」决定，由「最长的那个任务」决定。**

> 🚶 **类比一下：** 就像排队办事，前面那个人办了一个超复杂的业务（20分钟），你只是交个表（5秒），但你得等他办完。

#### 三、代码耦合 —— 牵一发而动全身

```c
while(1) {
    scan_key();
    update_led();
    uart_send();
    sensor_read();
    // 想加功能？在这里加一行
    // 想删功能？在这里删一行
    // 所有东西粘在一起！！！
}
```

- 所有功能耦合在同一个 while 里
- 一个模块崩了 → while 卡住 → 整个系统死掉
- 新人看代码：一个 while 里几十个函数调用，想死 😇

#### 四、功耗控制难

做电池设备的时候就哭了。想让 CPU 闲下来省电吧，又怕睡过去醒不来。结果芯片明明没活干，也只能一直跑着。

#### 总结——痛在哪？

| 痛点 | 一句话 |
|------|--------|
| CPU 浪费 | delay 在空转，没干正事 |
| 实时性差 | 紧急事件得等前面的跑完 |
| 代码耦合 | 所有功能塞在一个 while 里 |
| 功耗难降 | CPU 不能闲下来 |
| 难扩展 | 加个功能牵一发动全身 |

那有没有什么东西能解决这些问题呢？当然有 —— 这就是 RTOS 登场的理由。

---

## 2. FreeRTOS 是什么

### 2.1 一句话 + 详细表格

> **FreeRTOS 是一个为单片机设计的、轻量级的、开源的实时操作系统内核。**
> 这也是我选择讲`freertos`的原因, 它是一种操作系统, 我曾经就想着裸机嵌入式和大家未来方向似乎没有很大关系, 但是`freertos`这种带有操作系统思想的工具, 或许对大家的帮助更大.

| 方面 | 内容 |
|------|------|
| **出身** | 2003 年由 Richard Barry 开发，后来被 Amazon 收购 |
| **名字含义** | **Free**（~~福瑞bushi~~,免费）+ **RTOS**（实时操作系统） |
| **语言** | 纯 C，移植性好 |
| **体量** | 内核最小只需要 **3~5 KB ROM**，RAM 几 KB 就能跑 |
| **芯片支持** | ARM Cortex-M、RISC-V、AVR、PIC、MSP430…… 几乎所有主流单片机 |
| **开源协议** | MIT，商用也没问题 |
| **生态** | 官方有 AWS IoT 集成，社区资源极其丰富，中文资料也多 |

以最经典的 STM32F103C8T6 为例：64KB Flash，20KB SRAM。FreeRTOS 内核占 3~5KB ROM，几百字节 RAM —— **剩下的空间还够写一大堆业务逻辑**。

### 2.2 市面上 RTOS 那么多，为啥选它？

1. **最轻量** —— 占资源是所有 RTOS 里最小的之一
2. **最普及** —— 资料最多，遇到问题一搜就有答案 ~~（虽然可能搜到的是 2015 年的老帖子）~~
3. **大厂背书** —— Amazon 维护，还在持续更新（TrustZone、SMP 多核支持）
4. **完全免费** —— 不像 uC/OS 商用要收费

> 说个不恰当的类比：FreeRTOS 在单片机 RTOS 里的地位，差不多就像 Linux 在服务器操作系统的地位。它不一定是最强最全的，但它是你**大概率会遇到的第一个**。

---

## 3. 核心设计思想（重点）

好，这部分是今天的主菜，我们一层一层往下剥 🧅

### Q: 等等，上面不是说裸机有那么多毛病吗？那 RTOS 是怎么治这些毛病的？

### A:

| 痛点 | RTOS 怎么解决 |
|------|---------------|
| CPU 浪费 | ✅ Blocked 状态主动交出 CPU（见 3.4） |
| 实时性差 | ✅ 抢占式调度，高优先级任务随时打断 |
| 代码耦合 | ✅ 每个功能独立成一个任务 |
| 功耗难降 | ✅ 空闲任务自动让 CPU 进低功耗 |
| 难扩展 | ✅ 加一个 `xTaskCreate` 就行 |

好，正式开始。

---

### 3.1 任务 —— 最基本的执行单元

**裸机**的时候：一个大 `while(1)` 里塞了一堆东西。

**RTOS 的思路**：加一层!

```c
// 裸机
while(1) {
    scan_key();
    update_led();
    uart_send();
}

// RTOS —— 加一层, 把一开始的循环整体, 拆成三个独立的任务
void task_key(void *pv) {
    while(1) { scan_key(); vTaskDelay(10); }
}

void task_led(void *pv) {
    while(1) { update_led(); vTaskDelay(50); }
}

void task_uart(void *pv) {
    while(1) { uart_send(); vTaskDelay(100); }
}
```

每个任务有自己的：
- **栈（Stack）** —— 存局部变量、函数调用
- **任务控制块（TCB）** —— 存优先级、状态、栈指针等信息
- **独立的逻辑** —— 互不干扰

那它如何代替**超级循环**呢?
接下来的分析中, 大家带着问题, 自会明晰

### 3.2 调度 —— 谁来跑、跑多久

任务这么多，CPU 只有一个，**听谁的？**

FreeRTOS 有三种调度方式：

| 调度方式 | 什么意思 | 类比 |
|----------|----------|------|
| **抢占式（Preemptive）** | 高优先级的任务就绪了，低优先级的立刻让 | 🏥 急诊插队 |
| **时间片轮转（Time Slicing）** | 同优先级轮流跑，每人分固定时长 | 🍚 食堂排队每人打一勺 |
| **合作式（Cooperative）** | 你不主动让，我就不抢 | 🧘 自觉让座 |

**FreeRTOS 默认就是抢占 + 时间片轮转**，大部分场景够用了。

### 3.3 状态切换 —— 任务都在干嘛

一个任务在任意时刻，一定处于以下四种状态之一：

```
┌───────────┐    运行态:
│  Running  │  ← 正在跑（占用 CPU）
|           |
├───────────┤    就绪态:
│   Ready   │  ← 准备好了，等 CPU 来宠幸
├───────────┤
|           |    阻塞态:
│  Blocked  │  ← 在等某件事（延时、等队列、等信号量）
|           |
├───────────┤    挂起态:
│ Suspended │  ← 被挂起，不参与调度
└───────────┘
```

状态之间怎么转换的？

```text
Ready     ──调度器选中──▶  Running
Running   ──时间片到／被抢──▶  Ready
Running   ──vTaskDelay／等资源──▶  Blocked
Blocked   ──延时到／资源到──▶  Ready
Running   ──vTaskSuspend──▶  Suspended
Suspended ──vTaskResume──▶  Ready
```
可在[状态切换](../excalidraws/状态切换.excalidraw)中, 仔细解释

---

### 3.4 ⭐ Blocked 状态 —— FreeRTOS 省 CPU 的关键

这个点我觉得最值得讲清楚，很多人学完了还在迷糊。

#### Q: 裸机的 delay 是什么？

#### A:

```c
while(1) {
    delay_ms(100);      // ← CPU 这 100ms 全在空转数数
    toggle_led();
}
```

单片机执行 `delay_ms(100)`，本质是 CPU 在一个循环里不断自增一个变量，直到计数值到。**这 100ms 里 CPU 不能响应任何其他任务**，因为你根本没写别的任务让它干。

#### Q: FreeRTOS 的 delay 又是什么？

#### A:

```c
void task_led(void *pvParameters) {
    while(1) {
        toggle_led();
        vTaskDelay(100);    // ← 任务说：我 Blocked 了，CPU 你拿去干别的
    }
}
```

`vTaskDelay(100)` 的执行流程：

```
① 调度器把这个任务从 Ready 队列 → 扔到 Blocked 队列
② 调度器立刻挑一个 Ready 的任务来跑
③ 那 100ms 里，CPU 在扫键盘、算数据、读传感器 —— 反正不是空转
④ 100ms 到了，这个任务自动回到 Ready 队列，等调度器下次选它
```

> 💡 **Blocked 状态的价值在于：当任务无话可做时，它主动交出 CPU，而不是让 CPU 闲着等它。省时间的不是『切换动作本身』，而是把『空等』变成了『干正事』。**

#### Q: 那「任务切换快」又省的是啥？

#### A:

省的是**切换本身的 overhead**：

```text
保存当前寄存器、栈指针
    → 找到下一个任务
    → 恢复它的寄存器、栈指针
```

FreeRTOS 在 ARM Cortex-M 上用 **PendSV 中断**实现，只需要几个汇编指令，**微秒级**完成。所以你不用担心切来切去浪费时间。

🤔 关于 PendSV 的原理，说实话我自己也是半懂不懂，后面启动流程那节会再聊到，这里先有个印象就行。(这句话是cc自己写的喵)

---

### 3.5 优先级反转 —— 一个经典坑

多任务调度看起来很美对吧？但它会引出一些反直觉的问题。

#### 场景

三个任务 + 一个共享资源：

| 任务 | 优先级 | 行为 |
|------|--------|------|
| 任务 H（High） | 🔴 最高 | 需要访问共享资源 |
| 任务 M（Medium） | 🟡 中等 | 纯计算，不碰共享资源 |
| 任务 L（Low） | 🟢 最低 | 先拿到了共享资源的锁 |

#### 踩坑流程

```text
① 任务 L 拿到锁，进入共享资源区
② 任务 H 就绪了 —— 优先级高，直接抢占 L，H 开始跑
③ H 跑了一会儿，想用共享资源，
   发现锁被 L 拿着 → H 被 Blocked
④ 任务 M 就绪了！M 优先级比 L 高 → M 抢占 L
⑤ M 跑得欢，L 一直没机会释放锁 → H 就一直等不到锁
⑥ 结果：最高优先级的 H，被中优先级的 M 间接踩在脚下 😵
```

> **最高优先级的任务，反而在等最低优先级的任务释放锁。** 这就是**优先级反转**。

#### 💡 FreeRTOS 怎么解决：优先级继承

FreeRTOS 的互斥量（Mutex）自带一个机制叫 **优先级继承**：

```
① H 因为等 L 的锁而被 Blocked
② FreeRTOS 暂时把 L 的优先级提升到 H 的级别
③ L 获得 CPU → 快速运行 → 用完资源 → 释放锁
④ L 释放锁后 → 优先级降回原来
⑤ H 拿到锁 → 继续跑
```

> 🏃 **一句话：** 「优先级反转就是『高等公民被中等公民插队气死』。优先级继承就是让低等公民临时获得高等身份，快速办完事让位。」

<span style="color:red">注：</span> 用 `xSemaphoreCreateMutex()` 而不是 `xSemaphoreCreateBinary()` 创建互斥量，才自动带优先级继承。~~别问我怎么知道的，踩过坑😇~~

---

### 3.6 任务间的同步与通信

多个任务之间怎么配合？FreeRTOS 提供了几种机制：

| 机制 | 干什么的 | 类比 |
|------|----------|------|
| **队列（Queue）** | 任务 A 发数据给任务 B | 📮 邮筒 —— A 往里扔，B 从里取 |
| **信号量（Semaphore）** | 通知某件事发生了（资源可用） | 🚦 红绿灯 —— 告诉你"可以走了" |
| **互斥量（Mutex）** | 同一时间只有一个任务能用某个资源 | 🔑 厕所钥匙 —— 谁拿谁进，别人等着 |
| **事件组（Event Group）** | 多个条件都满足才触发 | ✅✅ 打卡 —— 两个人都到了才出发 |

核心概念就一句话：**谁等谁、谁通知谁。**

### 3.7 FreeRTOS 的设计哲学

- **内核最小化**：只做调度和同步，不绑协议栈、文件系统、设备驱动。你要这些？自己去加。
- **可裁剪**：`FreeRTOSConfig.h` 里一堆 `#define`，用哪个开哪个，不用就注释掉，省 ROM。
- **面向嵌入式**：支持静态内存分配，你不用非得用 `malloc`（~~单片机没有 MMU，malloc 用起来跟裸奔一样危险~~）

---

## 4. FreeRTOS vs 现代操作系统

这个视角挺有意思的，可以开阔一下思路。

### 现代 OS 能做啥？

- 多任务 / 进程管理
- 文件系统
- 网络协议栈
- 虚拟内存 / MMU
- 设备驱动框架
- GUI 系统

### FreeRTOS 实现了其中哪些？

| 能力 | FreeRTOS | 现代 OS（Linux/Windows） |
|------|----------|-------------------------|
| 多任务 | ✅ 抢占式 + 时间片 | ✅ 优先级 + 时间片 |
| 同步/通信 | ✅ 队列、信号量、互斥量 | ✅ 管道、信号量、共享内存 |
| 内存管理 | ✅ 简单的 heap 方案（5种） | ✅ 虚拟内存 / 分页 |
| 上下文切换 | ✅ 寄存器级 | ✅ 进程上下文 + TLB 刷新 |
| 中断管理 | ✅ 延迟中断处理（ISR + 任务配合） | ✅ 内核态中断处理 |
| 文件系统 | ❌ 不内置 | ✅ 标配 |
| 网络协议 | ❌ 不内置（可选 Plus/TCP） | ✅ 标配 |
| 虚拟内存 | ❌ 没有 MMU，用不了 | ✅ 核心能力 |
| 用户/内核态隔离 | ❌ 内核和任务在同一个地址空间 | ✅ 核心安全机制 |
| GUI | ❌ 不提供 | ✅ 各系统不同 |

### 值得讲的点

- FreeRTOS 就是一个**精简到只剩多任务核心**的微型 OS 内核
- 它没有用户态/内核态隔离：所有任务跑在同一个地址空间（因为单片机本身就是跑在特权模式，没有 MMU 来隔离）
- 没有 MMU → 没有虚拟内存 → 也没有进程隔离 → **一个任务写数组越界，能把整个系统干崩** 😇
- 但也因此足够轻量，RAM 几 KB 就能跑起来
- Linux 的调度器目标是"公平"，FreeRTOS 的调度器目标是"准时" —— 这就是"实时"俩字的含义

> 所以 FreeRTOS 不是"缩水版的 Linux"，**它是针对不同场景重新设计的东西**。

---

## 5. 启动与文件结构

### 5.1 完整执行流

单片机从上电到跑起 FreeRTOS，历经这么几步：

```text
① 上电 → 芯片从 Flash 取出 Reset_Handler 地址
② Reset_Handler 执行：
      - 复制 .data 段（初始化的全局变量）到 RAM
      - 清零 .bss 段（未初始化的全局变量）
      - 设置堆栈指针 SP
③ 跳转到 SystemInit（配置系统时钟、PLL 锁相环）
④ 跳转到 main()
```

这些是编译器 + 芯片启动文件帮你做的，~~你基本不用管（但面试可能会考）~~，知道一下就行。

### 5.2 main() 里面发生了什么

这是最值得展开讲的一段：

```text
main()
  │
  ├── 外设初始化（GPIO、UART、定时器等）
  │
  ├── 创建任务
  │     xTaskCreate(Task1, "Task1", 128, NULL, 1, NULL);
  │     xTaskCreate(Task2, "Task2", 128, NULL, 2, NULL);
  │     ↑ 注意！这只是在内存里建了个任务控制块（TCB）
  │       任务还没开始跑，只是注册了一下
  │
  ├── vTaskStartScheduler()   ← 这一行是分水岭！
  │     │
  │     ├── 创建空闲任务（Idle Task）—— CPU 没事干就待在这
  │     ├── 如果有软件定时器 → 创建定时器服务任务
  │     ├── 初始化系统时钟节拍（SysTick，一般是 1ms 中断一次）
  │     ├── 启动第一个任务（手动触发 PendSV 中断）
  │     └── 不再返回！！！
  │
  └── 永远不会执行到这里
```

> 🔑 **一句话：** 「`vTaskStartScheduler()` 就像按下了一个开关，从此不再是你的 `main` 在控制 CPU，而是调度器在管。就像交了班，你自己站到旁边去了。」

### 5.3 vTaskStartScheduler() 之后的世界

| 事件 | 谁触发 | 干啥 |
|------|--------|------|
| **SysTick 中断** | 硬件定时器（1ms 一次） | 更新任务延时计数 + 触发调度决策 |
| **PendSV 中断** | 调度器软件触发 | 保存旧任务上下文 → 加载新任务上下文 |
| **SVC 中断** | 任务调用 FreeRTOS API | 从用户模式切到内核模式执行系统调用 |

系统跑起来以后，就是一个**中断驱动**的世界：
- **SysTick** 像心跳，1ms 跳一次，告诉调度器"该看看有没有任务等完了"
- **PendSV** 像调度器的"换人按钮" —— 需要切换任务时就触发它
- **SVC** 像"系统调用" —— 任务想调用 FreeRTOS 的服务时就触发它

### 5.4 为什么非要用中断做切换？

Q: 直接用函数调用不行吗？

```c
void vTaskSwitch(void) {
    // 手动切换……
}
```

为什么不能这样？🤔

#### A:

```text
如果用普通函数调用：
    vTaskSwitch() → 切换到下一个任务...
    → 问题：函数调用会压栈（压到当前任务的栈里），
      切换完栈指针都指向新任务的栈了，
      return 回到哪去了？没法搞。

用 PendSV 中断：
    → 中断有硬件压栈（自动保存 R0-R3, R12, LR, PC, xPSR）
    → PendSV 优先级设最低，不会和真正的中断抢
    → 中断返回时自动恢复新任务的上下文
    → 完美衔接
```

**所以 PendSV 是 ARM Cortex-M 上 RTOS 的基础设施**，不是 FreeRTOS 独有的，别的 RTOS 也这么干。

### 5.5 代码文件对应关系

| 文件 | 作用 | 一句话 |
|------|------|--------|
| `tasks.c` | 任务创建、调度、状态管理 | **FreeRTOS 的心脏** |
| `list.c` | 链表操作（调度器内部队列管理） | 调度器手里的「花名册」 |
| `queue.c` | 队列和信号量的实现 | 任务之间的「传话筒」 |
| `timers.c` | 软件定时器 | 到点自动触发，不用自己写计时 |
| `event_groups.c` | 事件组 | 多个条件都满足才触发 |
| `FreeRTOSConfig.h` | 裁剪配置文件，决定开哪些功能 | **减肥菜单** —— 不用的就注释掉，省 ROM |
| `port.c / portmacro.h` | 移植层（不同芯片适配） | 换芯片就换这一个文件夹 |
| `heap_x.c` | 内存管理方案（heap_1 ~ heap_5） | 5 种分配策略，选一个适合你的 |

---

## 写在最后

如果你以前只写过裸机程序，第一次接触 FreeRTOS 可能会觉得："不就是把 while 拆成几个小 while 吗，有多了不起？" ~~（我以前就是这么想的）~~

但当你真的跑起来、踩过优先级反转的坑、用队列解决过任务通信问题之后，你会感受到：

> **RTOS 不是一种技术选择，而是一种思维方式的转变。**
> 从「我在管 CPU」变成「调度器在管，我只管写任务」。

当然，RTOS 不是银弹。裸机在某些简单场景下更直接、更可控。~~而且 RTOS 的调试比裸机痛苦多了 😇~~

但当你面对一个功能越来越多、实时性要求越来越高的项目时，FreeRTOS 会是你最好的帮手之一。

---

*以上就是本次例会的分享内容，谢谢大家 🙌*

> 本笔记如果有讲错的地方欢迎指正喵
